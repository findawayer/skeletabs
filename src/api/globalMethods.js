import { setClassNames } from '../core/classNames';
import { setDefaults } from '../core/options';

// Methods that affects all Skeletabs instances.
// @example: $.skeletabs.setDefaults(myDefaults);
export default {
  setClassNames,
  setDefaults,
};
